name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x, 20.x]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Lint
      run: npm run lint

    - name: Test
      run: npm run test -- --ci --passWithNoTests

    - name: Build
      env:
        NODE_ENV: production
        NEXT_PUBLIC_SUPABASE_URL: https://aozwgkgeigdgfvbiuixe.supabase.co
        NEXT_PUBLIC_SUPABASE_ANON_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImFvendna2dlaWdkZ2Z2Yml1aXhlIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTAzNTQ3OTUsImV4cCI6MjA2NTkzMDc5NX0.5vkq74wyiX_4DF3_7j2_KzVTb5aVrsKa3iOnFT7MALU
      run: npm run build
